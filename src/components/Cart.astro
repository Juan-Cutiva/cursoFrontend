<section class="flex justify-center mt-10 m-6">
    <article class="flex-col">
        <a class="hover:scale-105 duration-200" href={Astro.props.href}
            ><img
                class="h-[20rem] w-full border-2 border-verde rounded"
                src={Astro.props.src}
                alt={Astro.props.alt}
            /></a>
        <h2 class="mt-4 flex justify-center font-medium">{Astro.props.text}</h2>
    </article>
</section>